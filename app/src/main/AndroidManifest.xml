<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.teklemariam.tekleandalc">
    <uses-permission android:name="android.permission.INTERNET"/>
    <!--<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity"
                    android:label="@string/activity_name_welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AboutALCActivity"
            android:label="@string/activity_name_aboutalc"
            android:parentActivityName=".WelcomeActivity"/>
        <activity android:name=".MyProfileActivity"
            android:label="@string/activity_name_myprofile"
            android:parentActivityName=".WelcomeActivity"></activity>
    </application>

</manifest>